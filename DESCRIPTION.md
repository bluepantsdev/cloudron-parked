A lightweight domain parking app for Cloudron.

Displays a branded landing page for each domain parked on your Cloudron instance. Supports multiple secondary domains (configured via the Cloudron admin (See Location)), each with its own customisable title, logo image, and blurb — set via a simple JSON config file stored in persistent app data.

Designed to be as lean as possible: static nginx serving, no database, no runtime framework.
